@using FGI.Enums
@model List<Lead>
@{
    ViewData["Title"] = "My Assigned Leads";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 text-gray-800">My Assigned Leads</h1>
        <div class="d-flex">
            <div class="badge bg-primary text-white rounded-pill p-2 ms-2">
                <i class="fas fa-users me-1"></i> @Model.Count Leads
            </div>
        </div>
    </div>

    <div class="card shadow mb-4">
        <div class="card-header py-3 bg-gradient-primary text-white">
            <h6 class="m-0 font-weight-bold">Leads Management</h6>
        </div>
        <div class="card-body">
            @if (Model.Count == 0)
            {
                <div class="alert alert-info d-flex align-items-center">
                    <i class="fas fa-info-circle me-3 fa-2x"></i>
                    <div>
                        <h5 class="alert-heading">No Leads Assigned</h5>
                        <p class="mb-0">You currently have no leads assigned to you.</p>
                    </div>
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-hover" id="leadsTable" width="100%" cellspacing="0">
                        <thead class="thead-light">
                            <tr>
                                <th>Client</th>
                                <th>Contact</th>
                                <th>Project</th>
                                <th>Unit</th>
                                <th>Type</th>
                                <th>Price</th>
                                <th>Area</th>
                                <th>Status</th>
                                <th>Created</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var lead in Model)
                            {
                                <tr>
                                    <td>
                                        <div class="font-weight-bold">@lead.ClientName</div>
                                    </td>
                                    <td>
                                        <a href="tel:@lead.ClientPhone" class="text-primary">
                                            <i class="fas fa-phone-alt me-1"></i> @lead.ClientPhone
                                        </a>
                                    </td>
                                    <td>@lead.Project?.Name</td>
                                    <td>@lead.Unit?.UnitCode</td>
                                    <td>@lead.Unit?.Type.ToString()</td>
                                    <td>@lead.Unit?.Price</td>
                                    <td>@lead.Unit?.Area</td>
                                    <td>
                                        <span class="badge @GetStatusBadgeClass(lead.CurrentStatus)">
                                            @lead.CurrentStatus
                                        </span>
                                    </td>
                                    <td>@lead.CreatedAt.ToString("dd MMM yyyy")</td>
                                    <td class="text-center">
                                        <a asp-action="LeadDetails" asp-route-id="@lead.Id"
                                           class="btn btn-sm btn-info me-1" title="View Details">
                                            <i class="fas fa-eye"></i>
                                        </a><!--
                                        <button type="button" class="btn btn-sm btn-primary"
                                                data-bs-toggle="modal" data-bs-target="#statusModal"
                                                data-lead-id="@lead.Id" data-lead-status="@lead.CurrentStatus">
                                            <i class="fas fa-edit me-1"></i> Update
                                        </button> -->
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
<!-- Status Update Modal -->
<!--
<div class="modal fade" id="statusModal" tabindex="-1" aria-labelledby="statusModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="statusModalLabel">Update Lead Status</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form asp-action="UpdateStatus" asp-controller="Sales" method="post" id="statusForm">
                @Html.AntiForgeryToken()
                <input type="hidden" name="leadId" id="modalLeadId" />

                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">Current Status</label>
                        <div class="p-2 rounded bg-light">
                            <span id="currentStatusBadge" class="badge"></span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="status" class="form-label">New Status *</label>
                        <select name="status" class="form-select" id="statusSelect" required>
                            @foreach (LeadStatusType status in Enum.GetValues(typeof(LeadStatusType)))
                            {
                                if (status != LeadStatusType.New)
                                {
                                    <option value="@status">@status</option>
                                }
                            }
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="comment" class="form-label">Comments *</label>
                        <textarea name="comment" class="form-control" id="commentText" rows="3" required></textarea>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-1"></i> Cancel
                    </button>
                    <button type="submit" class="btn btn-primary" id="saveStatusBtn">
                        <i class="fas fa-save me-1"></i> Save Changes
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
-->

@functions {
    string GetStatusBadgeClass(LeadStatusType status)
    {
        return status switch
        {
            LeadStatusType.New => "bg-primary",
            LeadStatusType.NoAnswer => "bg-warning",
            LeadStatusType.FollowUp => "bg-info",
            LeadStatusType.Busy => "bg-secondary",
            LeadStatusType.Canceled => "bg-dark",
            LeadStatusType.DoneDeal => "bg-success",
            LeadStatusType.NotInterested => "bg-danger",
            LeadStatusType.WrongNumber => "bg-light text-dark",
            LeadStatusType.Closed => "bg-dark",
            _ => "bg-light text-dark"
        };
    }
}

@section Styles {
    <style>
        .card-header {
            border-radius: 0.35rem 0.35rem 0 0 !important;
        }

        .badge {
            font-size: 0.8em;
            font-weight: 500;
            padding: 0.5em 0.75em;
        }

        .table-hover tbody tr:hover {
            background-color: rgba(0, 0, 0, 0.02);
        }

        .status-badge {
            min-width: 100px;
            display: inline-block;
            text-align: center;
        }
    </style>
}
@section Scripts {
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- DataTables core JS -->
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>

    <!-- DataTables Bootstrap 5 integration -->
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script>

                $(document).on('submit', '#statusForm', function(e) {
            e.preventDefault();

            var $form = $(this);
            var $btn = $('#saveStatusBtn');

            // Disable button and show loading state
            $btn.prop('disabled', true);
            $btn.html('<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Saving...');

            $.ajax({
                url: $form.attr('action'),
                type: 'POST',
                data: $form.serialize(),
                success: function(response) {
                    if (response.success) {
                        toastr.success(response.message);

                        // Add slight delay before redirect
                        setTimeout(function() {
                            if (response.redirectUrl) {
                                window.location.href = response.redirectUrl;
                            }
                        }, 1000);
                    } else {
                        toastr.error(response.message);
                        $btn.prop('disabled', false);
                        $btn.html('<i class="fas fa-save me-1"></i> Save Changes');
                    }
                },
                error: function(xhr) {
                    toastr.error('An error occurred while updating status');
                    $btn.prop('disabled', false);
                    $btn.html('<i class="fas fa-save me-1"></i> Save Changes');
                    console.error('Error:', xhr.responseText);
                }
            });
        });
        });
    </script>
}